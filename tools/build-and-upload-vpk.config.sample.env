# Optional FTP target settings for build-and-upload-vpk scripts.
# Missing or empty values fallback to defaults in scripts.
# Build artifacts are always written to <repo>/build.

FTP_HOST=192.168.1.20
FTP_PORT=1337
FTP_REMOTE_DIR=ux0:/homebrews
